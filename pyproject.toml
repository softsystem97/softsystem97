[project]
name = "softsystem97"
authors = [{ name = "SoftSystem97", email = "contact@softsystem97.com" }]
description = "SoftSystem97 — Portail client, Stripe webhook, notifications, finance"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
	# Installed and managed by bench / Frappe Cloud.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "softsystem97"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = ">=14.0.0,<=17.0.0-dev"
erpnext = ">=14.0.0,<=17.0.0-dev"
